// type definitions for Cypress object "cy"
/// <reference types="cypress" />

describe('Login Page', () => {
  it('should test cypress setup', () => {
    expect(true).be.true;
    expect(true).not.to.be.eq(false);
  });
});
